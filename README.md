To change width, height, and mines, you have to go into src/main.c and change the constants at the top.
To run the game, build with ./build.sh and then run it with ./bin/Debug/Minesweeper

Plan on adding functionality to change width, height, and mines in game
Might break on monitors that aren't 16:9
Your times are saved in resources/times.json
Currently a highscores.json file is created, but empty. Will add that later

The game is fully working. left click to dig. right click to flag. left click on number to chord.
